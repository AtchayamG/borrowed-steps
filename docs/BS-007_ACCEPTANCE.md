# BS-007 backend acceptance - 2026-09-09
STATUS: ACCEPTED_BACKEND; full local integration recorded separately in M2B_INTEGRATION.md.
Worker6ad75bc, followed by Codex test-only commit b371b7c. Production backend unchanged by Codex.
Independent verification: original380 tests PASS; Ruff lint/format and strict mypy PASS. Added two real SQLite regressions: injected event failure rolls back the task transition, and human pickup between discovery and claim prevents a due event. Both PASS; integrated canonical suite382 PASS, Ruff/mypy PASS. Initial reviewer monkeypatch targeted a slotted instance and was corrected to the class; no application failure.
Accepted StorageBusyError as a port exception (unchanged generic HTTP failure), composite workspace/loan FK, immediate pickup notice, migration-time backfill timestamp/opaque IDs, stable created_at/id ordering, and owned bounded runner. These implement the frozen contract without a new architecture decision.
Worker smoke evidence reviewed, not independently rerun as that same script; independent combined real browser proof is recorded separately. Existing worker tests establish migration/backfill, uniqueness/isolation, repeat/restart, lock contention, boundaries and actual thread start/stop. Added reviewer cases cover rollback and a concrete discovery/action interleaving.
Known test limitations: the worker's stop-between-candidates test starts with no candidates, and its cleanup test observes runner state rather than a drain spy. They do not alone prove those claims; source finally/stop paths, other runner tests and combined server shutdown evidence supply the relevant assurance. No claim of final security or production acceptance.
Historical assistant-smoke test clock-rollover flake was reported by Claude, not independently reproduced here; defer isolated test-clock stabilization until that retired harness is in scope. It does not authorize reopening any inference allocation.
No application correction loop, dependencies, model calls, spend, push or public deployment. Frontend accepted at5ce9611. Canonical combined source checked separately.

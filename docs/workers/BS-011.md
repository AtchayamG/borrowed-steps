# BS-011 — Codex takeover completion

TASK_ID: BS-011
WORKER: Codex direct takeover of interrupted Claude work
MODEL: User-selected Codex/Astra; hidden serving identity not independently verified
EFFORT: ASTRA_HIGH (user confirmed)
STATUS: COMPLETED — bounded PostgreSQL foundation accepted by Codex
BRANCH: worker/codex/BS-011-takeover
BASE_COMMIT: 12b6a59

## Implemented and verified

Completed Claude's two partial PostgreSQL files in a separate worktree, leaving
his checkout and running database untouched. Added explicit migration CLI,
pinned driver overlay, actual PostgreSQL acceptance tests and setup guidance.
Constructor performs no I/O or migrations. Workspace write locking, repeatable
read-only snapshots, database constraints, exact idempotency and connection
cleanup implement the frozen M3 contract without changing the inner layers.

All seven task acceptance criteria pass for this foundation scope:

1. Existing Store/UoW protocol methods implemented. Explicit version readiness,
   fresh/concurrent/repeated migrations and transactional DDL rollback verified.
2. Worktree-local Python 3.11.15 environment; psycopg and psycopg-binary 3.2.10
   pinned in a base-lock overlay. Existing manifests unchanged.
3. Real PostgreSQL 16.10 verifies workspace isolation, allocation/idempotency
   contention, UTC persistence, due ordering/claims and atomic event behavior.
4. Parameterized SQL, bounded connections/locks/statements, conservative
   prepared-statement disabling, verified TLS outside explicit loopback,
   sanitized failures and connection closure after success/error/timeout.
5. Lifecycle/expiry, duplicate mutation replay/conflict, injected rollback,
   due-processor races, pickup/return interleaving, database constraint backstops,
   and fresh-process persistence pass against real disposable databases.
6. Existing use cases tested directly. HTTP factory and accepted SQLite path
   untouched; no hosted HTTP proof or public deployment claimed.
7. Full suite: **431 passed, zero failed/skipped**, including 413 retained tests
   and 18 new cases (13 real-database cases and 5 offline URL cases). Ruff lint
   and format pass; strict mypy passes 68 files; 72 installed packages compatible.

TESTS_RUN: exact commands and sanitized results in
`services/agent/test-evidence/bs011/acceptance.json`; reproduction in
`docs/POSTGRES_SETUP.md`. The initial full run's one existing subprocess failure
was a missing editable install in the new environment; corrected without
changing that test or source. Two inherited Starlette deprecation warnings.

FILES_CHANGED: postgres_store.py, postgres_migrations.py, scripts/postgres_migrate.py,
requirements-postgres.lock, tests/test_postgres_store.py, test-evidence/bs011/acceptance.json,
docs/POSTGRES_SETUP.md, this report and the four task checkpoints.

KNOWN_ISSUES: Hosted factory/provider/admission/scheduler integration and free
account verification remain future gates. Schema version 1 does not migrate
existing SQLite records. No live model or cloud calls; all29 prior allowances
remain closed. No spend, push or deployment.

ARCHITECTURE_DEVIATIONS: None. URI validation deliberately requires verify-full
outside loopback and rejects query routing overrides. Prepared statements
disabled without asserting that every pooler lacks support.

REVIEW_REQUEST: Codex takeover reviewed and locally accepted; integrate only the
bounded foundation files, preserving accepted BS-012 and all earlier history.
NEXT_SAFE_ACTION: Record canonical integration and resume manual orchestration
for the next hosted integration task. Do not restart Phase 0 or rerun Claude's
interrupted task against this checkout.

NEXT_CODEX_MODE: ASTRA_LIGHT
REASON: Foundation implementation and substantive verification are complete.
